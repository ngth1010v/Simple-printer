# TASK:
- viết cho tôi 6 file ConfigModel/, ConfigParser.h/cpp, ConfigWriter.h/cpp để làm trung gian giữa exe và ini + giữ data config với các yêu cầu bên dưới

# CONFIG.ini CẦN LƯU:
[Basic]
priner=Canon LBP 3018

[Advance]
printMode=Manual Duplex (Flip On Short Edge)
paper=A4
scale=Fit to page (keep aspect ratio)
orientation=Auto
collate=Collated (1,2,3 | 1,2,3 | 1,2,3)

[Margin]
top=1
bottom=1
left=1
right=1



# OUTPUT:
- ConfigModel.h (full)
- ConfigParser.h (full)
- ConfigParser.cpp (full)
- ConfigWriter.h (full)
- ConfigWriter.cpp (full)

# MAIN IDEA:
- ConfigWriter:
+ chỉ dùng để nhận data từ Config.cpp và write config vào .ini bằng winapi
+ nếu file chưa tồn tại -> tự tạo
- ConfigParser:
+ chỉ dùng để đẩy data từ .ini và write config vào Config.cpp bằng winapi
+ nếu không có file -> trả về config::ConfigData gốc (tức giá trị được kkhai báo sẵn ở trong ConfigModel)
- ConfigModel:
+ dùng để giữ struct data

# EXPORT FUNCTION:
- ConfigModel
+ struct config::ConfigData {
    string printer = "Microsoft print to PDF"
    string printMode=Simplex
    string paper=A4
    string scale=Fit to page (keep aspect ratio)
    string orientation=Auto
    string collate=Collated (1,2,3 | 1,2,3 | 1,2,3)
    int[4] margin = {1,1,1,1} 
}
- ConfigParser
+ config::ConfigData config::Parse(string path)
- ConfigWriter
+ void config::Write(string path, ConfigData data)


# NOTE:
- nếu có thiếu thông tin, hãy hỏi kĩ tôi trước khi code